[phases.setup]
nixPkgs = ["python311Full", "freefont_ttf", "dejavu_fonts", "fontconfig"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 main.py"
